<%= simple_form_for(@moo) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :context, label: "Content" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
